<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login_id %>
    <%= form.number_field :login_id, id: :book_login_id %>
  </div>

  <div class="field">
    <%= form.label :route_id %>
    <%= form.number_field :route_id, id: :book_route_id %>
  </div>

  <div class="field">
    <%= form.label :no_tickets %>
    <%= form.number_field :no_tickets, id: :book_no_tickets %>
  </div>

  <div class="field">
    <%= form.label :phone_no %>
    <%= form.number_field :phone_no, id: :book_phone_no %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :book_email %>
  </div>

  <div class="field">
    <%= form.label :total_fare %>
    <%= form.text_field :total_fare, id: :book_total_fare %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
